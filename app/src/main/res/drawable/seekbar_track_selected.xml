<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 轨道颜色 -->
    <solid android:color="@color/color302AI" />
    <!-- 轨道高度（即厚度） -->
    <size android:height="6dp" />
    <!-- 轨道圆角（可选） -->
    <corners android:radius="3dp" />
</shape>