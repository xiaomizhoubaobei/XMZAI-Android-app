# iFlow 项目规范文档

## 文件头注释规范

所有源代码文件（TypeScript/JavaScript）必须在文件开头添加标准化的文件头注释。

### 注释模板

```typescript
/**
 * @fileoverview [模块功能简述]
 * @author [作者姓名]
 * @date [创建日期]
 * @since [起始日期]
 * @LICENSE AGPL-3.0 license
 * @remark [详细功能说明]
 *
 *          该模块实现了以下功能：
 *          - [功能1]
 *          - [功能2]
 *          - [功能3]
 *
 *          工作流程：
 *          1. [步骤1]
 *          2. [步骤2]
 *          3. [步骤3]
 *
 *          示例：
 *          - [示例1]
 *          - [示例2]
 */
```

### 字段说明

| 字段              | 说明                     | 必填 |
|-----------------|------------------------|----|
| `@fileoverview` | 模块功能的简短描述              | 是  |
| `@author`       | 作者姓名                   | 是  |
| `@date`         | 文件创建日期 (YYYY-MM-DD)    | 是  |
| `@since`        | 功能起始版本/日期 (YYYY-MM-DD) | 是  |
| `@LICENSE`      | 许可证类型                  | 是  |
| `@remark`       | 详细的功能说明、工作流程、使用示例      | 是  |

### 使用示例

```typescript
/**
 * @fileoverview 请求代理和国际化路由处理模块
 * @author 祁筱欣
 * @date 2026-02-22
 * @since 2026-02-22
 * @LICENSE AGPL-3.0 license
 * @remark 本模块处理 Next.js 应用的请求代理和国际路由功能。
 *          该模块实现了以下功能：
 *          - handleLangParam(): 处理 URL 中的 lang 参数，将其转换为路由路径并重定向
 *          - proxy(): 主入口函数，协调语言参数处理和 i18n 路由
 *          - 支持的语言：中文(zh)、英文(en)、日文(ja)
 *
 *          工作流程：
 *          1. 首先检查 URL 中是否包含 lang 参数，如果包含则重定向到对应的语言路由
 *          2. 然后调用 next-intl 的中间件处理常规的 i18n 路由
 *          3. 只处理根路径和以支持语言代码开头的路径
 *
 *          示例：
 *          - /?lang=zh → 重定向到 /zh
 *          - /en?lang=zh → 重定向到 /zh
 *          - /page?lang=ja → 重定向到 /ja/page
 */
import { NextResponse, type NextRequest } from "next/server";
```

### 注意事项

1. **许可证类型**：本项目使用 AGPL-3.0 许可证，所有文件头注释中的 `@LICENSE` 字段应统一为 `AGPL-3.0 license`
2. **日期格式**：统一使用 `YYYY-MM-DD` 格式
3. **注释语言**：建议使用中文编写 `@remark` 字段的内容
4. **代码块**：在 `@remark` 中提到函数或变量时，使用 `functionName()` 或 `variableName` 格式
5. **示例清晰**：提供清晰的输入输出示例，帮助其他开发者快速理解功能